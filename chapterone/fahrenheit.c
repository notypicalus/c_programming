 1 #include <stdio.h>

/* print Fahrenheit-Celsius table
for fahr = 0, 20, ..., 300 */
int main()
{
    float fahr, celsius;
    int lower, upper, step;
    lower = 0;
    upper = 300;
    step = 20;
    
    fahr = upper
    while (fahr <= upper)
    {
	celsius = 5 * (fahr, celsius);
	printf("%d\t%d\n", fahr, celsius);
	fahr = fahr + step;
    }
}
